# Καθαρή ανακατασκευή βάσης BetAnalytic

Η διαδικασία **δεν καθαρίζει την υπάρχουσα βάση επί τόπου**. Δημιουργεί νέα βάση σε απομονωμένο φάκελο, ξαναφορτώνει τις επιτρεπτές πηγές, εκτελεί ελέγχους ποιότητας και αντικαθιστά το `betanalytic.db` μόνο όταν δεν υπάρχουν κρίσιμα σφάλματα.

## Τι ελέγχεται

- ξένα κλειδιά SQLite,
- διπλότυποι αγώνες με ίδιο φυσικό κλειδί,
- ολοκληρωμένοι αγώνες χωρίς τελικό σκορ,
- διαφορετικές ομάδες, ημερομηνία ή σεζόν ανάμεσα σε αγώνα και στατιστικά,
- μεικτές πηγές,
- στατιστικά σε μη ολοκληρωμένο αγώνα,
- σουτ στην εστία περισσότερα από τα συνολικά σουτ,
- αρνητικές τιμές,
- χειροκίνητες επαληθεύσεις που έχουν καταχωριστεί στο `data/manual_verified_matches.json`.

Οι προβληματικές εγγραφές στατιστικών μπαίνουν σε καραντίνα και αφαιρούνται από:

- το `fixture_statistics`,
- το `fixture_history_details`,
- το `data/fixture_statistics.json`,
- το feed και το μοντέλο προβλέψεων.

Δεν αντικαθίστανται με εικασίες.

## Εκτέλεση από GitHub Actions

1. Actions → **Rebuild clean database**.
2. Run workflow.
3. Στο πεδίο επιβεβαίωσης γράφεις ακριβώς `REBUILD`.
4. Περιμένεις να ολοκληρωθεί πράσινο.
5. Μετά εκτελείται αυτόματα το **Update and publish BetAnalytic data**.

## Αρχεία ελέγχου

- `data/clean_rebuild_report.json`: συνολικό αποτέλεσμα ανακατασκευής.
- `data/manual_verification_report.json`: αποτελέσματα χειροκίνητων ελέγχων.
- `data/quarantined_statistics.json`: εγγραφές που αποκλείστηκαν.
- `data/manual_verification_queue.csv`: όλες οι υπόλοιπες εγγραφές προς χειροκίνητο έλεγχο.

Το artifact του workflow περιέχει και αντίγραφο ασφαλείας της προηγούμενης βάσης.
